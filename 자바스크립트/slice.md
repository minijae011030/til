## 1️⃣ slice()란?
- **slice()는 배열(Array)이나 문자열(String)의 일부를 잘라서 새로운 값으로 반환하는 메서드다.**
- 원본을 변경하지 않는다 (불변성 유지)
- 부분 복사에 사용

## 2️⃣ 기본 문법
```
array.slice(start, end)
string.slice(start, end)
```

파라미터	설명
- start	시작 인덱스 (포함)
- end	끝 인덱스 (미포함, 생략 가능)

## 3️⃣ 배열에서 slice()
### 🔹 기본 사용
```
const arr = [1, 2, 3, 4, 5];

const result = arr.slice(1, 4);
console.log(result); // [2, 3, 4]
console.log(arr);    // [1, 2, 3, 4, 5]
```

### 🔹 end 생략
```
arr.slice(2); // [3, 4, 5]
```

### 🔹 음수 인덱스
```
arr.slice(-2); // [4, 5]
arr.slice(1, -1); // [2, 3, 4]
```
